# Change Log — PSX Quantitative Research Platform

> **Purpose:** Records every material change to the research workspace — documents created, revised, deprecated, or restructured. Provides an audit trail of how the platform evolved.  
> **Related:** [Research_Governance.md](Research_Governance.md) · [Model_Registry.md](Model_Registry.md)

---

## Log Format

Each entry:
- Date (ISO 8601)
- Type: `Create` / `Revise` / `Deprecate` / `Restructure` / `Finding`
- Document(s) affected
- Brief description of change

---

## 2026-07-01 — Platform Foundation

**Type: Create**
**Session:** Initial research workspace build (two-session process)

### Session 1 — Workspace Scaffolding (original 8 documents)
- Created: `Vision.md` — strategic intent and principles
- Created: `README.md` — initial navigation document
- Created: `Claude_Findings.md` — system audit findings from code review
- Created: `Build_Roadmap.md` — phased development plan
- Created: `Research_Log.md` — session journal
- Created: `Data_Dictionary.md` — database schema and column definitions
- Created: `Decisions.md` — key platform decisions record
- Created: `Questions.md` — open questions tracker

*Workspace relocated from `/research` to `ZH_research/` (user preference).*

### Session 2 — Research Conventions (added 2 documents)
- Created: `Hypotheses.md` — hypothesis register with RQ→H traceability
- Created: `Evidence_Register.md` — evidence record following RQ→H→S→E→D chain

### Session 3 — Complete Documentation Architecture (this session: 32 documents created)
- Created: `Naming_Conventions.md` — ID systems, prefixes, status values
- Created: `Research_Governance.md` — decision authority, prohibited practices
- Created: `Research_Standards.md` — 10 research standards
- Created: `Research_Workflow.md` — 8-step research process
- Created: `Statistical_Guidelines.md` — statistical methods standards
- Created: `Bias_Checklist.md` — bias review protocol
- Created: `Validation_Framework.md` — 4-tier validation architecture
- Created: `Evidence_Standards.md` — confidence level criteria
- Created: `Reproducibility_Policy.md` — reproducibility requirements
- Created: `Outcome_Definitions.md` — OV-01 through OV-07 specifications
- Created: `Factor_Catalog.md` — F-01 through F-42 complete factor inventory
- Created: `Factor_Taxonomy.md` — 4-dimension factor classification
- Created: `Factor_Interaction_Matrix.md` — IX-01 through IX-T03 interaction register
- Created: `Market_Regime_Framework.md` — regime classification and research rules
- Created: `Sector_Framework.md` — sector data structure and research rules
- Created: `Data_Quality_Policy.md` — pre-study quality gates and known data issues
- Created: `Known_Limitations.md` — L-01 through L-13 standing limitations register
- Created: `Assumption_Register.md` — A-01 through A-18 validated and unvalidated assumptions
- Created: `Conviction_Engine_Specification.md` — full engine design specification
- Created: `Probability_Framework.md` — how probabilities are derived and used
- Created: `Score_Evolution_Roadmap.md` — V0 through V3 engine versions
- Created: `Acceptance_Criteria.md` — 6-gate deployment acceptance test
- Created: `Failure_Criteria.md` — FC-01 through FC-05 kill switches
- Created: `Model_Registry.md` — version registry for all engine versions
- Created: `Research_Pipeline.md` — active and queued study tracking
- Created: `Research_Backlog.md` — unscheduled ideas and proposals
- Created: `Historical_Similarity_Design.md` — design spec for analogue lookup feature
- Created: `Research_Glossary.md` — complete terminology reference
- Created: `Research_Review_Checklist.md` — 8-section pre-close study review gate
- Created: `Future_Research_Ideas.md` — long-horizon speculative ideas
- Created: `Documentation_Style_Guide.md` — writing standards
- Created: `Change_Log.md` — this document

**Total documents in workspace after Session 3:** 32

---

## Future Entries

*Record every change here going forward. Include the date, type, document(s), and a one-line description.*

| Date | Type | Document | Change |
|---|---|---|---|
| 2026-07-01 | Create | Master_Research_Plan.md | Primary governing document for the 12–24 month research program |
| 2026-07-03 | Finding | Hypotheses.md, Evidence_Register.md, S-001_Pivot_Distance_BREAKOUT.md | H-001 (`pivot_distance_pct` for BREAKOUT) marked `Terminated` — Development-era null (Cliff's delta -0.0023, all 4 regimes individually null) fails pre-registration's cross-era independence requirement. Logged as `TRO-001` in Evidence_Register.md's new "Tested and Ruled Out" section. Added new `Terminated` status definition to Hypotheses.md. |
| 2026-07-03 | Revise | S-001_Pivot_Distance_BREAKOUT.md | Added Addendum C — explicit methodology-change record for the mid-study pivot from fixed-band [4,8]%→[-8,-4]% to Development-quintile-exploration→Q4-vs-Q1 direct contrast; consolidates what/when/why per the guardrail against silent methodology drift. |
| 2026-07-03 | Create | Questions.md (Q-001) | Logged open question on the unexplained Validation-only regime pattern for the (terminated) H-001 Q4/Q1 effect — explicitly not a hypothesis; requires new pre-registration as H-002 if pursued. |
| 2026-07-03 | Create | Hypotheses.md (H-002, H-003) | Registered H-002 (`rs_score_20` Q4 vs Q1) and H-003 (six-flag EMA/Stage family, Bonferroni α=0.0083) **before** any Development-era query was executed — both hypotheses replace the terminated H-001. Corrected a stale "H-002" placeholder reference in H-001's notes and Questions.md Q-001 that predated this registration and now points to the wrong ID. |
| 2026-07-03 | Finding | Hypotheses.md (H-003), Evidence_Register.md | H-003 Development gate resolved per-flag: `overhead_clear` Provisional Accept (p=0.000758, Cliff's δ=0.0309), proceeding to Validation. Other 5 flags (`stage2_bull`, `close_above_ema150`, `close_above_ema50`, `ema150_slope_pos`, `ema50_slope_pos`) Terminated at Development gate — logged as TRO-002 through TRO-006. |
| 2026-07-03 | Finding | Hypotheses.md (H-002) | H-002 Development result recorded (Cliff's δ=-0.0496; two-sided p significant, but pre-registered one-sided direction Q4>Q1 not significant). Status set to "Development complete, PI review pending" — held for directional clarification before Validation is opened. |
| 2026-07-03 | Create | Decisions.md (D-002) | New standing rule: binary flags with minority-class population share below ~5% are excluded from standalone hypothesis testing in future candidate pools (may still be used as stratifiers/controls). Motivated by `close_above_ema50` (0.37%) and `ema50_slope_pos` (1.07%) in the H-003 candidate pool. |
| 2026-07-03 | Finding | Hypotheses.md (H-002), Evidence_Register.md | H-002 formally closed: `rs_score_20` Q4-vs-Q1 pooled-regime one-sided test rejected at Development (p=0.999853, wrong direction). Logged as TRO-007. Never opened for Validation/OOS. |
| 2026-07-03 | Create | Hypotheses.md (H-004) | Registered H-004 (`rs_score_20` Q4 vs Q1, TRENDING_UP regime only) **before** any Development-era query was executed — PI-authorized, explicitly a new hypothesis distinct from the terminated H-002, not a re-test. Scope restricted to TRENDING_UP only; extension to other regimes requires separate new pre-registration per D-001. |
| 2026-07-03 | Create | Hypotheses.md (H-005, H-006, H-007) | Registered three new hypotheses **before** any Development-era query was executed: H-005 (Volume Expansion Ratio, N=20 fixed, with the volume-adjustment uncertainty logged as a standing limitation in the registration text itself); H-006 (sector `breadth_score`, with a required — not optional — sector-concentration check and explicit L-03 exclusion accounting pre-registered as part of the Development report); H-007 (Coil-Tightening Slope, N=20 fixed, Q1-vs-Q5 extreme-quintile convention). H-007's lineage disclosure corrected a PI-cited ID: `TRO-001` is `pivot_distance_pct`, not BBW%/`base_tightness` — the BBW% dead finding still has no formal TRO entry (outstanding backfill item, not fabricated here). |
| 2026-07-03 | Finding | Hypotheses.md (H-005, H-006, H-007), Evidence_Register.md | Development gate resolved for all three: H-005 (VER) Provisional Accept (Cliff's δ=0.0690, one-sided p<0.000001), proceeding to Validation. H-006 (sector breadth) flagged confounded by the pre-registered, required sector-concentration check (top-5 share 43.95%→61.28% across Q1→Q5) — held for sector-controlled re-test, not marked Accept/Terminate. H-007 (Coil-Tightening Slope) Terminated (Cliff's δ=-0.0134, wrong-direction null) — logged as TRO-008, with explicit lineage disclosure to the still-unlogged BBW%/`base_tightness` dead finding. |
| 2026-07-03 | Create | Decisions.md (D-003) | Candidate 4 (Gap-at-Breakout) formally shelved — 0% Development-era `open`-price coverage makes it structurally incompatible with the project's Development-first pre-registration discipline, independent of any backfill effort. Not revisited without a separate PI decision to backfill or to authorize a non-standard Validation-start protocol. |
| 2026-07-04 | Create | Hypotheses.md (H-008) | Registered H-008 (Gap-at-Breakout) in a **BLOCKED** state, before any query — `prices.open` was backfilled locally by the PI (2026-07-04), but `prices_adjusted.open` is still NULL (Phase 5 corporate-action adjustment pending) and no EXP-0002-style reconstruction/look-ahead audit has been run on it. No query was issued against `prices.open` or `prices_adjusted.open` for any purpose. Supersedes D-003's shelving rationale (0% coverage) now that raw backfill has occurred, but introduces two new, separate unblock gates. |
| 2026-07-04 | Finding | Hypotheses.md (H-005) | Corrected a paperwork gap: the Validation result (Cliff's δ=0.0445, one-sided p=0.004360), executed and reported as raw output in the prior round, had not been written back into the Status field — fixed before OOS was opened. OOS then executed (final, single query, no recalibration): Cliff's δ=**-0.0037**, one-sided p=0.611131, recorded as raw result only — no final Confirmed/Terminated verdict applied; awaiting PI/reviewer review. |
| 2026-07-04 | Finding | Hypotheses.md (H-006) | PI elected to **park** H-006 — sector-controlled re-test showed the effect concentrated in 2 of 5 top sectors (Commercial Banks, Textile Composite); not advanced to Validation. May be revisited as a narrower, freshly pre-registered hypothesis if PI chooses. |
| 2026-07-04 | Finding | Hypotheses.md (H-005), Evidence_Register.md | H-005 (VER) formally **Terminated at OOS gate** — reviewed and closed on the three-era decay trajectory (Cliff's delta 0.0690 → 0.0445 → -0.0037), not the isolated OOS null in isolation. Logged as TRO-009, explicitly documented as a monotonic-decay closure pattern distinct from a single-era null (contrast with TRO-001/007/008). OOS was final per protocol; no recalibration performed. |
| 2026-07-04 | Finding | Hypotheses.md (H-003), Evidence_Register.md | Caught and corrected a paperwork gap while closing out BREAKOUT: H-003's `overhead_clear` Validation result (reported as raw output in an earlier round) had never been written back into Hypotheses.md, leaving H-003 shown as "In Progress." Closed using the already-collected numbers (no new query) — direction reversed from Development (δ=+0.0309) to Validation (δ=-0.0666), terminated at Validation gate. Logged as TRO-010. H-003 is now genuinely fully closed (all 6 flags terminated). |
| 2026-07-04 | Create | Evidence_Register.md (BREAKOUT Research Status Summary) | Added a closing summary for BREAKOUT research. Corrected the proposed "six terminated hypotheses" framing on verification: the true count is **five** hypotheses tested to completion and terminated (H-001, H-002, H-003, H-005, H-007); H-004 was registered but **never executed** (no Development query was ever run) and is explicitly excluded from the terminated count, not folded in. H-006 (Parked) and H-008 (Blocked) also excluded, correctly labeled as their own distinct statuses. Documented as "deprioritized, not abandoned" per PI framing, citing all relevant TRO IDs. |
| 2026-07-04 | Create | BREAKOUT_Specification_v1.0.md | Added as the new **authoritative conceptual reference** for the BREAKOUT construct, produced via structured chart elicitation with the PI (Phase 1 Market Phenomenon + Phase 2 Formal Specification), independent of the database implementation and independent of trade outcome. Source examples: NETSOL (2016-02-08), HBL (2017-05-25), UBL (2017-12-21), POL (2017-10-03). Defines the breakout event (Section 2) and active resistance (Section 1) as trader-judgment concepts, explicitly excluding trend/quality/outcome factors from the definition itself (deferred to later Conviction-factor research phases). **Supersedes informal, ad-hoc references to the "state vs. event" concern raised earlier in the project** — that concern is now formally captured in Section 6, which states BREAKOUT V1 (H-001 through H-008, the state-based `bos_flag=1 AND avg_vol_10d>200000` construct) remains a valid finding *for the construct it evaluated*, and does not transfer to this new specification (BREAKOUT V2) without a fresh, dedicated audit (Phase 3 — not yet started). Per explicit instruction, Hypotheses.md, Evidence_Register.md, and all H-001–H-008 records were **not modified** — those findings stand as recorded. |
| 2026-07-15 | Create | Triangle_Pattern_Specification_v1.md | New versioned spec, permanent reference for the pivot→trendline→triangle-classification pipeline built and validated this session (project-root modules `pivots.py`, `boundaries.py`, `triangle.py`, plus research-pipeline-only `research_filters.py` — none of these live in `ZH_research/`, only this spec does). Documents 5 bugs found via direct real-data/adversarial review, in order: frozen-close pivot collapsing (TOWL/ISIL), boundary silent-fallthrough (`fit_boundary` fabricating touch claims on an unrefit line), placeholder-zero data artifact in `prices_adjusted` (1,596 rows/205 symbols — production fix intentionally out of scope, tracked separately), three flatness-definition iterations (per-bar rate → touch-span → window-based), and the `is_forced_line()` touch-spacing gate (cross-referenced to ChartSchool's Trend Lines article). Final real-data result: 45 candidates classified as triangles from 177 converging windows before the forced-line filter; **10** after (the forced-line gate rejects 71% of candidates, by far the dominant filter). Test suites: `test_pivots.py` 10/10, `test_boundaries.py` 8/8, `test_triangle.py` 16/16, `test_research_filters.py` 4/4. |
| 2026-07-15 | Revise | Triangle_Pattern_Specification_v1.md (Section 6.1) | Breakout-occurrence counting refined from 3 categories (up/down/never) to 5. Initial pass resolved same-horizon dual-boundary-violation candidates (3 of 10) to "whichever broke first" — reviewing the raw bar-gap between the two violations (`KEL 3160-3240`=31, `TOWL 360-440`=18, `TOWL 1840-1920`=3) showed the 3-bar case is structurally different from the other two, so first-occurrence resolution was replaced before being recorded as final. Decision: `whipsaw` (gap ≤5 bars, reusing `is_forced_line()`'s existing `min_pairwise_gap_bars` rather than a new constant) vs. `sequential, both directions` (gap >5 bars — two temporally distinct events, not force-resolved into a single direction) are now separate categories. Final: up=3, down=4, whipsaw=1, sequential=2, none=0. `breakout_occurrence_count.py` updated to match (`OUTCOME_CATEGORIES` now 5-valued; a pandas `None`-vs-`NaN` bug in the interim single-direction classification, introduced while adding this logic, was caught and fixed in the same pass). |
| 2026-07-15 | Revise | Triangle_Pattern_Specification_v1.md (new Section 7, old Section 7→8) | Documented the 18-ticker widening (5→18, same 5 sectors as the original tickers, 3-4 per sector across liquidity tiers): funnel (1,916 windows → 47 classified triangles), 5-category breakout result (up=14, down=15, whipsaw=3, sequential=15, none=0), and 5 findings flagged during widening (market-cap-vs-trading-frequency mismatch on `UPFL`, sequential share rising 20%→32%, a new 1-bar-gap whipsaw on `KOHC` confirmed genuine, `DNCC`'s low-price regime confirmed genuine, placeholder-zero artifact confirmed on 4 more symbols). Closed with 3 pre-full-universe follow-ups: (1) liquidity tier redefined by trading-day coverage (≥90%/70-89%/<70%) instead of market cap, documentation-only, confirmed to touch no pivot/boundary/triangle logic; (2) sequential-outcome share spot-checked on 4 of 15 cases (`PPL`, `POL`, `KTML`, `DNCC`) — every violation sat 7+ bars clear of the horizon cutoff, confirmed genuine market behavior, not a horizon-truncation artifact; (3) semi-log vs arithmetic price scale tested directly on `DNCC`/`OGDC` windows — naive log-transform broke `fit_boundary()`'s tolerance formula (scales with log-magnitude, not price; degenerated all 5 `OGDC` test windows to `not_a_triangle_shape`), confirmed arithmetic scale's existing relative tolerance already addresses the original motivating concern — **arithmetic retained, no pipeline change.** |
| 2026-07-15 | Create | `research_filters.py` (`KNOWN_ARTIFACT_SYMBOLS`, `exclude_known_artifact_symbols()`) | New standing, upfront, symbol-level exclusion for `SGPL`/`DWAE`/`FRCL` — the near-zero adjustment-factor artifact first found and rejected during 5-ticker selection (Section 4), re-confirmed present (min positive close 0.0001, all three) in the full 305-symbol universe run. Explicit decision: exclude at the symbol level before any pivot/boundary/triangle work starts, for every future classical-pattern study reusing this universe (rectangles next), not a per-row filter like `drop_placeholder_zero_bars()`. 7/7 tests in `test_research_filters.py` (was 4/4). |
| 2026-07-15 | Revise | Triangle_Pattern_Specification_v1.md (new Section 8, old Section 8→9) | Documented the full 305-symbol active-universe run (funnel: 27,006 windows → 545 classified triangles; 5-category breakout: up=206, down=188, whipsaw=23, sequential=126, none=2 — `none` appears for the first time at this scale) and 5 data-quality findings (SGPL/DWAE/FRCL's artifact re-confirmed at scale; 122 symbols now show the placeholder-zero artifact vs. 6 previously known; `MWMP`/`OML` checked and confirmed genuine ultra-low-price stocks, not artifacts; `ANLNV` correctly produced zero windows, 56 rows total; 6 new gap=2 whipsaws, `MCB` spot-checked and confirmed genuine ~6x-volume market action). Recorded the exclusion decision (8.5) and the confirmed delta from re-running with `SGPL`/`DWAE`/`FRCL` excluded (8.6): −1 classified triangle (`SGPL 1480-1560`, symmetrical, its own breakout outcome was `sequential`) — 544 total, 302 symbols, closing out the full-universe stage. Sequential-outcome share across all three stages: 20% (N=10) → 32% (N=47) → 23.1%/23.0% (N=545/544) — the 32% figure was a small-sample high point, not the settling value. |
| 2026-07-15 | Create | `breakout_classification.py` (`classify_breakout()`) | New module: Type 1-4 post-breakout classification (Kibar/TechCharts framework, Brandt's LFD concept). Confirmed via direct fetch of the source article (not assumed) that the LFD reference mirrors by direction — low of the last full day for an up-breakout, high for a down-breakout. Documented deviation from the source: negation level collapsed to equal the LFD level (source treats them as two separate thresholds), per explicit instruction. Two touch conventions: boundary touch/breach uses the study's standard `tolerance_pct` band; LFD touch is exact/zero-tolerance (a literal stop-order level, not a fitted line). 12/12 initial tests, including both requested edge cases (exact-tolerance touch; LFD touch on the last bar of the horizon). |
| 2026-07-15 | Finding | `breakout_classification.py` | Original full-horizon LFD-touch window (up to ~45 bars) found too broad on the 544-candidate canonical set: Type 4 fired on 375/544 (69%), including 54.4% of clean `up` and 62.2% of clean `down` breakouts. Root cause: Kibar's LFD low is an initial-risk reference only, meant to be superseded by trailing-stop methods once a trade develops — not tested indefinitely. |
| 2026-07-15 | Revise | `breakout_classification.py` (`lfd_variant` parameter) | Added two candidate fixes, tested side by side, neither picked yet: `fixed` (LFD checked only N bars post-breakout, tested at 5 and 10) and `trigger` (LFD live only until close reaches the pattern's own measured-move target, then superseded). 17/17 tests (5 new, including one edge case per new variant — a touch exactly at the fixed-window cutoff, and a touch exactly on the trigger bar itself — plus a fix to two flawed test values that had accidentally collided the LFD level with the boundary-breach threshold, confirmed via direct inspection to be a test-construction bug, not a `classify_breakout()` bug). Real-data 4-way comparison (`current`/`fixed_5`/`fixed_10`/`trigger`) on all 544 candidates: `fixed_5` cuts the up/down false-Type-4 rate most (~39%) but degrades the sequential→Type4 mapping most severely (98.4%→54.4%); `fixed_10` and `trigger` land in a similar middle ground on both axes (up/down Type4 ~43-52%, sequential mapping ~74-75%). |
| 2026-07-15 | Create | `breakout_classification.py` (`DEFAULT_LFD_VARIANT = "trigger"`) | **Locked decision:** `trigger` set as the default `lfd_variant`, chosen over `fixed_10` because its cutoff derives from the pattern's own measured-move height rather than an arbitrary bar count, consistent with every other threshold in this study being defined relative to the pattern's own geometry. `full` and `fixed` remain directly callable for comparison. Existing/prior callers relying on the old `full` default (e.g. `breakout_classification_analysis.py`) now pin `lfd_variant="full"` explicitly to stay runnable and preserve their originally-documented full-horizon baseline numbers, rather than silently picking up the new default or crashing (the new default requires `closes`/`measured_move_height`). Test suite: 19/19 (2 new regression tests confirming the default really is `trigger`, not silently `full`). **Explicitly carried forward, not resolved by this decision:** even under `trigger`, 46.6% of `up` and 52.1% of `down` breakouts still touch their own LFD level before the trigger fires — flagged as an open, unquantified finding for the stop-loss/R-multiple stage, not fixed here. `Triangle_Pattern_Specification_v1.md` Section 9 added (old Section 8 Out-of-Scope renumbered to 10, `Type 1-4 classification` removed from the out-of-scope list since it's now in scope and documented). |
| 2026-07-15 | Create | Stop-loss / R-multiple comparison — definitions-lock stage started | Next module after `breakout_classification.py`. Per this project's established discipline (definitions locked → math spec → code → synthetic tests → real-data re-run), 4 design questions raised before any code: (1) Zeeshan's "usual stop method" in codeable terms — needs his input, `kiran_sim.py`/project memory checked first per instruction rather than assumed; (2) R (risk) definition — entry price ambiguity between breakout-bar close and boundary-touch price, needs explicit confirmation since LFD vs. "usual method" may imply different entries; (3) scope — recommended excluding Type-4-under-trigger from the primary stop-comparison (agreed-failed trades under either method), reporting Type 4 separately as "cost of the LFD stop specifically" per the Section 9.5 carried-forward finding; (4) target-side confirmation — measured-move achievement vs. fixed-horizon outcomes (both already computed), which is primary for realized R-multiple. No implementation yet — answers/findings to be reported before any code, same as every prior module. |
| 2026-07-16 | Revise | Triangle_Pattern_Specification_v1.md (new Section 10, old Section 10→11) | Stop-loss / R-multiple comparison stage complete — closes out the triangle study's core research arc (build → scale validation 5→18→302 tickers → measured-move analysis → Type 1-4 classification → stop-loss comparison). LFD, rolling-low, and opposite-boundary stops tested via bar-by-bar walk-forward on the n=230 primary set (Type-4-under-trigger excluded); found a clean Type-conditional split (LFD win rate 56.2%→41.5%→20.0% across Type 1→2→3, wider stop favored specifically on hard-retest trades, holding even on eventually-successful Type 3 candidates); the "sequential"-outcome subset's apparent opposite-stop edge (54.8% wins) shown to be delay (max 13 bars, median 5), not survival. An adaptive LFD-to-opposite switching rule (switch on pattern-boundary breach before LFD touch) was built and tested as a candidate real-time implementation of the Type-conditional finding — it underperformed simply staying on static LFD on the 26 trades where it triggered (mean realized_R 0.076 vs. 0.207; 8W/12L/6T), closed as a negative sub-finding: the Type-conditional split should inform ex-ante stop selection at entry, not a mid-trade switching rule. |
| 2026-07-16 | Finding | `temporal_split_validation.py`, `loss_winner_concentration.py` | Full-sample EV claim (n=518 primary population, gross +1.277R/+1.28%/trade, net of friction+CGT +0.853%/trade) tested against the two checks this project's discipline requires before any EV claim is trusted. **Both fail it.** Temporal holdout (split 2017-02-07): Era A net +1.762%/trade, Era B net **−0.070%/trade** (net-negative) — the pooled number averages a real edge with a dead one. Concentration check: 47/518 candidates (9.1%) have realized R > 5.0 ("large winners"); removing exactly these 47 flips the result from +1.277R to **−0.730R**. Same run also found the descending/ascending kind asymmetry reverses entirely between eras (Era A: descending mean +312.4% vs ascending +29.9% of measured-move-@90d; Era B: ascending +287.8% vs descending **−2010.9%**) — logged as a second, independent illustration of why single-era findings on this dataset are not trustworthy without a holdout check. |
| 2026-07-16 | Finding | `kibar_trend_filter.py`, `kibar_filter_ev_retest.py` | Kibar's 200-day trend filter (SMA/EMA-alignment gate) tested two ways. Naive check (raw percent-move-among-survivors across 5 horizons) looked roughly neutral — mixed improves/worsens by horizon. Proper check (realized R-multiple EV, same n=518 population, 343/503 SMA-eligible candidates pass at 68.2%) tells a different story: net-of-cost EV roughly halves (+0.853%/trade unfiltered → +0.387%/trade filtered), era-instability is not fixed (filtered Era A mean R 1.19 vs Era B 0.24, same order-of-magnitude gap as unfiltered), and outlier concentration gets *worse*, not better (top 5 trades = 94.4% of the filtered population's total summed R). Logged specifically because the naive raw-move check would have shipped a "neutral, maybe keep it" conclusion — the R-multiple check is what actually matters and says worsens. Filter rejected. |
| 2026-07-16 | Finding | `fixed_pct_stop_target.py`, `fixed_pct_comparison.py` | Fixed 2%-stop/4%-target scheme tested against the LFD-dynamic scheme used everywhere else in this study, same n=518 population. Structurally solves the outlier-concentration problem found above: top-5 concentration drops from ~94-99% (LFD-based) to 32.3% unfiltered / 38.5% with the Kibar filter, win rate rises to 35.3%/35.9% (vs 18.1%/18.7%). But the resulting edge (mean R +0.0598 unfiltered, +0.0758 filtered — +0.06%/+0.08%/trade) is too thin to survive realistic costs: for comparison, the LFD scheme lost ~0.4 percentage points moving from gross to net-of-friction-and-CGT off a much larger +1.28%/trade base. Not separately cost-adjusted — the gross number alone is already inside the noise band. Closed as a real structural fix (concentration) that does not produce a tradeable edge. |
| 2026-07-16 | Finding | `lfd_reentry_chain.py`, `lfd_reentry_chain_comparison.py` | Re-entry after an LFD stop-out tested (chained trades, capped at 3 re-entries; cap bound in 21.7% of chains). 115/230 (50.0%) of the n=230 primary set re-entered at least once. Restricted to those 115 re-entered chains (where, by construction, the original single trade was already a loser — median realized R = −1.0): chain (all legs summed) median realized R drops to −2.0 to −2.71 (worse than the original single loss), chain beats the original outcome in only 39/115 (34%), worse in 76/115 (66%), zero ties. Closed: re-entering after a stop-out more often compounds the loss than recovers it — median trader would have been better off walking away. |
| 2026-07-16 | Restructure | `Triangle_Pattern_Specification_v1.md` — full rewrite, study formally CLOSED | Superseded the incrementally-built v1 document with a single standalone, reference-grade rewrite (not an appended section) synthesizing the entire study: pivot→trendline→triangle detection (5→18→302-ticker scale validation), Type 1-4 breakout classification, stop-loss/R-multiple comparison, and the five 2026-07-16 EV-validation findings logged above (temporal holdout, concentration check, kind-asymmetry reversal, Kibar filter retest, fixed 1:2 scheme, re-entry chains — the adaptive-switching finding was already in the prior version and carried forward unchanged). New document structure: (1) Overview, (2) Reusable infrastructure — `pivots.py`/`boundaries.py`/`triangle.py`/`research_filters.py`/`breakout_classification.py`/`stop_loss_definitions.py`, with `breakout_classification.py` and `stop_loss_definitions.py` explicitly flagged as GENERAL (no triangle-specific logic — reusable as-is for rectangles), each with one paragraph on design decisions and known limitations (including the rolling-low stop's ~25% coverage on triangle candidates, confirmed via `stop_loss_r_multiple_comparison.py` re-run: 58/230 ok, 143/230 skipped risk>6%, 29/230 no valid consolidation), (3) the one validated finding (Type-conditional stop selection, confirmed independently in both temporal eras, framed as an ex-ante entry-time judgment aid, not a mid-trade rule), (4) all six rejected findings with actual numeric reasons, (5) final verdict — **triangles as specified are NOT a demonstrated tradeable edge on PSX; the infrastructure is real and reusable; the profitability claim should not be revived without a genuinely new angle**, (6) six meta-lessons for the rectangles study, each tied to a specific mistake made and caught in this study. **Study status: CLOSED.** No further triangle-pattern work is planned; next pattern in the queue is rectangles, per the standing exclusion decision already recorded in Section 8.5 of the prior document version (now folded into the new Section 2's `research_filters.py` writeup). |
| 2026-07-16 | Create | `Rectangle_Pattern_Specification_v1.md` | New study opened — definitions-lock and math-spec design stage only, **no code, no tests, no data run**, matching the triangle study's own pre-coding discipline. Sourced definitions from ChartSchool (2-touch minimum, close-based breakout, <3wk=flag/~3mo ideal duration), CMT-adjacent material (substantively identical), and Aksel Kibar/@TechCharts (confirmed via direct fetch of his X post and two blog posts, not assumed): **3-touch minimum** (stricter than ChartSchool, explicitly stated and adopted as this study's locked default), explicit horizontal-over-diagonal preference ("stop drawing diagonal lines... focus on horizontal boundaries"), close-based breakout confirmation (agrees with ChartSchool), and real examples running 6mo–5yr+ (materially longer than ChartSchool's "ideal ~3mo" and than any triangle in the prior study). Central design decision made explicit and recorded, not defaulted silently: rectangle boundaries will be fit via a new dedicated horizontal-level-clustering function (pinning slope=0 by construction) rather than reusing `fit_boundary()`'s free-slope Theil-Sen/OLS fit and classifying flatness post-hoc — chosen because the latter path can satisfy an aggregate flatness threshold without ever genuinely passing through 3 touches at one consistent price, which doesn't match Kibar's stated method. Reuse audit against every triangle-study module, confirmed piece-by-piece by reading the actual code (not assumed from the task's characterization): `pivots.py` (fully reusable), `boundaries.py`'s `Boundary` dataclass and `check_violation()` (fully reusable, `fit_boundary()` itself deliberately not reused for this pattern), `triangle.py`'s `is_forced_line()` (fully reusable — no slope dependency) and calendar-day duration convention (reusable), `compute_apex()`/`classify_triangle()` (not applicable — rectangles have no apex and, per ChartSchool's "neutral pattern" framing, no directional sub-kind the way triangles' ascending/descending/symmetrical split has), `breakout_classification.py` and `stop_loss_definitions.py` (confirmed fully reusable with zero modification, since both operate on generic `Boundary`/`highs`/`lows`/`closes` with no triangle-specific assumption), `research_filters.py` (fully reusable, already scoped for "rectangles next" in the closed triangle spec). Six open questions flagged explicitly for confirmation before any code is written: horizontal-fit-vs-degenerate-triangle path, touch-minimum default (3 vs 2), tie-break rule for level-candidate selection, `tolerance_pct` validity for this pattern, `min_height_pct` (a new rectangle-specific degenerate-pattern guard with no triangle analog), and `MIN_RECTANGLE_DURATION_DAYS`/windowing parameters (explicitly flagged that the triangle study's 80-bar/50%-overlap windowing should NOT be reused by default, given rectangles' apparently much longer typical duration). Measured-move price targets (emphasized by both sources) explicitly deferred, not locked, to match the triangle study's own staged discipline. |
| 2026-07-16 | Revise | `Rectangle_Pattern_Specification_v1.md` (new Section 3.2, Section 6 Q6 resolved) | **Q6 (windowing/duration scheme) resolved** — the hub question, confirmed first per its own flagged interaction with Q2/Q4/Q5. Calibration input from the Independent Quantitative Reviewer's direct PSX trading experience: rectangles run "no more than a couple of months" before breaking out or being abandoned, treated as more relevant than Kibar's own stated 6mo–5yr range (his markets, likely more liquid/slower-moving than PSX). Re-checked, not assumed, whether triangles' 80-bar/50%-overlap scan might actually be closer to appropriate given the shorter PSX-specific ceiling: 80 bars ≈ 3.8 calendar months for a fully liquid name (same rough order as "a couple of months"), but 6-9+ months for mid/thin-tier names (`UPFL`-character, <70% trading-day coverage) — reuse still rejected, but on a sharper, re-derived rationale than the original note gave: a fixed bar-count window is inherently unstable against calendar time on this universe, the identical instability that already justified `triangle.py`'s calendar-day (not bar-count) duration field, now shown to apply to the scanning mechanism itself. **Confirmed design: adaptive, pivot-anchored, breakout-closing growing window** (not a fixed-width scan) — chosen because rectangles lack triangles' self-bounding geometric property (the apex), so a fixed-width scan risks splitting a genuine pattern across two overlapping windows at arbitrary phase offsets, each independently failing the touch minimum despite the full pattern clearing it; triangles were never exposed to this because triangle geometry doesn't depend on where you slice it. Confirmed numbers: `MIN_RECTANGLE_DURATION_DAYS = 21` (ChartSchool's flag/rectangle cutoff, sourced not estimated) and `MAX_RECTANGLE_DURATION_DAYS = 60` primary (a literal "couple of months" reading), with `90` flagged as an empirical fallback to test if 60 starves thin-tier candidates of enough calendar time to reach the 3-touch minimum — not a competing default, a contingency. Two new interactions surfaced beyond the original Q6→Q2/Q4/Q5 notes: adaptive vs. fixed (not just duration length) affects how achievable the touch minimum is in practice (Q2), and an adaptive window's height is measured between genuine first-touch/breakout points rather than an arbitrary slice, likely making `min_height_pct` calibration less noisy (Q5). Two mechanical details (anchor-pivot selection rule, overlapping-candidate deduplication) flagged as a follow-on design pass, not new numbered open questions. Q1, Q2, Q3, Q4, Q5 remain open, to be resolved one at a time — Q1 next, since it was already identified as gating Q2 and Q3. |
| 2026-07-16 | Revise | `Rectangle_Pattern_Specification_v1.md` (new Section 2.1, Section 6 Q1 resolved) | **Q1 (horizontal-fit vs. degenerate-triangle path) reconsidered against the newly-confirmed adaptive windowing design, not just re-confirmed as-is.** Original reasoning (Section 2) was framed entirely around a fixed window with two arbitrary edges; re-derived under adaptive windowing's pivot-anchored start. Found: anchoring the start to a real pivot partially, not fully, weakens the original aggregate-flatness argument (guarantees one genuine touch, but says nothing about interior-point clustering or the non-touch breakout-bar edge) — **on reconsideration, the original argument was somewhat overstated on its own.** Found a new, stronger, adaptive-growth-specific argument not present in the original reasoning: incremental-fit stability. Adaptive growth requires evaluating boundary validity as new pivots arrive incrementally — fixed-window triangles never faced this. A free-slope Theil-Sen/OLS fit's slope can drift as new points are added during growth, potentially invalidating an already-accepted "flat" window retroactively (no clean resolution: roll back, or freeze the fit and stop re-checking, defeating the purpose). A considered alternative (refit from scratch every growth step) doesn't help — same risk, strictly worse cost (full O(k²) Theil-Sen recomputation per pivot vs. once per fixed window). The horizontal-pinned fit has no slope to drift, so this problem cannot arise for it. **Path B reaffirmed, with the primary justification now incremental-growth stability, not the original aggregate-flatness argument** — a genuine re-derivation, not a restatement. Flagged for Q2/Q3 (next): the anchor pivot is touch #1 by construction for whichever boundary it anchors, changing how both questions should be framed. |
| 2026-07-16 | Revise | `Rectangle_Pattern_Specification_v1.md` (Section 3 revised, Section 6 Q2/Q3 resolved) | **Q2 and Q3 resolved together**, since Q1's anchor-pivot mechanic directly affects both. **Q2 (touch minimum):** reconsidered against the confirmed adaptive design rather than assumed unchanged. Found a genuine boundary asymmetry: the anchor pivot supplies one boundary's first touch for free by construction; the *other* (non-anchored) boundary must accumulate all `min_touches` organically inside the same short 21-60 day window, with no equivalent head start — meaning the adaptive design's efficiency gain (no wasted pre/post-pattern window time) only partially transfers, and whether 3 touches are realistically reachable on the non-anchored side within 21-60 days is an empirical question the reasoning-only analysis could not settle. **Confirmed: 3 stays the default/primary-reported value (unchanged rationale — aligning with Kibar over ChartSchool/CMT), but `min_touches=2` runs as a co-equal comparison on the first real-data pass, not a fallback.** `min_touches` remains caller-supplied either way. **Q3 (tie-break rule), reframed in the process of resolving it:** the original framing ("tie-break among candidate starting levels") implicitly assumed a fixed window handing `fit_horizontal_level()` a complete up-front pivot set. Under the confirmed anchored design, the anchored boundary has no tie-break question at all — its only candidate is the anchor pivot's price, by construction; the question only ever genuinely applies to the *non-anchored* boundary's early-growth phase, before enough same-kind pivots accumulate for one candidate's inlier-count dominance to become clear. **Confirmed rule: tightest inlier price spread**, chosen over earliest-index pivot because adaptive growth should let accumulating evidence determine the level as the window matures, not lock onto whichever pivot arrived first — matches `boundaries.py`'s existing residual-minimizing precedent. Section 3's `fit_horizontal_level()` procedure rewritten to describe the anchored/non-anchored split explicitly (anchored boundary skips steps 1-3 entirely, seeded directly from the anchor). Q4 and Q5 remain open, to be resolved together next (both already flagged as needing to stay mutually consistent with each other). |
| 2026-07-16 | Restructure | `Rectangle_Pattern_Specification_v1.md` — definitions-lock stage CLOSED, all six questions resolved | **Q4 and Q5 resolved, closing out the rectangle definitions-lock stage.** Q4 (`tolerance_pct`): confirmed reuse of `0.015` unchanged — adaptive vs. fixed windowing has no bearing on it (tolerance is a pure price-magnitude test, independent of time/window construction); the one real uncertainty is duration-driven (21-60 day windows vs. triangles' ~4mo) and direction-ambiguous from reasoning alone (could argue tighter or looser), flagged for real-data validation rather than resolved. Q5 (`min_height_pct`): derived rather than borrowed. The tolerance-band non-overlap condition gives a mechanical floor `height_pct > 2*tolerance_pct/(1-tolerance_pct) ≈ 0.0305` at `tolerance_pct=0.015` — revealing the original placeholder (`0.03`, borrowed from `triangle.py`'s unrelated `flat_cumulative_pct`) sat *below* this floor and would have shipped internally inconsistent (tolerance bands touching, no real separation) if not caught. **Confirmed as a formula, not a hardcoded constant:** `min_height_pct = margin_multiplier * (2*tolerance_pct/(1-tolerance_pct))`, `margin_multiplier=2.0` → `≈0.061` (~6.1%) — keeps `min_height_pct` automatically consistent if `tolerance_pct` is later revised, rather than requiring two independent placeholders to be manually re-checked against each other. Confirmed flat (not duration-scaled): the failure mode it guards against (near-coincident levels, frozen-close-artifact false levels) is a price-magnitude property, not a time one. **New Section 7 added: "Definitions Lock — Final," a consolidated one-table summary of all six resolutions (Q1-Q6) with pointers to full reasoning** — old Section 7 (Explicitly Out of Scope) renumbered to Section 8. Document status line updated: definitions-lock stage marked CLOSED, every locked item explicitly flagged as a proposal pending real-data validation, not a final constant. **Next stage: synthetic test design (hand-verified cases) and isolated code for `fit_horizontal_level()` and `assemble_rectangle()`** — no code has been written yet for this study. |
