# Kiran Local-First Migration -- archive / Medallion tooling deps.
# Kept out of requirements.txt (Streamlit Cloud, being retired) and
# requirements-dev.txt (CI). Install locally for archive work:
#     pip install -r requirements-archive.txt
#
# pyarrow is already a transitive dep (pandas) and does the Parquet export.
pyarrow>=17
# boto3 -- S3-compatible client for the Backblaze B2 Object-Lock push
# (archive/offsite_push.py, archive/restore_drill_archive.py). Pure Python,
# wheels for 3.14. Not in requirements.txt: the retired Streamlit Cloud app
# never touches B2.
boto3>=1.35
#
# duckdb is the Phase 3 Medallion engine (query/compute; pyarrow stays the
# Parquet serializer -- see docs/KIRAN_LOCAL_FIRST_ARCHIVE/MEDALLION.md).
# 2026-09-10: duckdb 1.5.5 installs + runs clean on this machine's Python 3.14.4,
# so the "no cp314 wheel" blocker the tracker/memory carried is resolved.
duckdb>=1.5
